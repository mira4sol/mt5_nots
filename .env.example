# Copy to .env and fill in values (never commit .env)
# Do NOT wrap values in quotes — Make exports them literally into the process env.

MT5_LOGIN=501086912
MT5_PASSWORD=MetaFxInv$$50
MT5_SERVER=ValetaxGlobal-Live3

WHATSAPP_TARGET=120363428584387160@g.us

MT5_BACKEND=bridge
MT5_BRIDGE_CLIENT=mt5linux
MT5_BRIDGE_HOST=127.0.0.1
MT5_BRIDGE_PORT=18812
MT5_TERMINAL_PATH=C:\Program Files\Valetax Global MT5 Terminal\terminal64.exe

# Note: In .env use $$ for a literal $ in passwords when using `make` (Make converts $$ → $)
# Or run python directly without make and use a single $ in the password.
